sudo: required
services: 
  - docker
before_install:
  - docker build -t praveen/docker-react  -f Dockerfile.dev .
script: 
   - docker run -e CI=true praveen/docker-react npm run test -- --coverage
 


  
